rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access for all users during development
    // Make sure to secure this before going to production!
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
