name: ustaad_flutter
description: Ustaad Agentic AI Orchestrator
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.9
  flutter:
    sdk: flutter
  image_picker: ^1.2.2
  provider: ^6.1.5+1
  shared_preferences: ^2.5.5
  supabase_flutter: ^2.12.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
